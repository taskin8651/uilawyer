@extends('layouts.admin')
@section('page-title', 'Legal Q&A')
@section('content')
<div class="admin-page-head"><div><h2 class="admin-page-title">Legal Q&A / AI Chat</h2><p class="admin-page-subtitle">Placeholder chat UI with saved Q&A history, ready for future AI API integration.</p></div></div>
<form method="POST" action="{{ route('admin.legal-qas.store') }}" class="form-card" style="margin-bottom:22px;">@csrf <div class="form-card-header"><div class="form-card-icon"><i class="fas fa-comments"></i></div><div><p class="form-card-title">Ask Legal Question</p><p class="form-card-subtitle">Saved answer currently uses a clean placeholder response.</p></div></div><div class="form-card-body"><textarea name="question" rows="4" class="field-input" required placeholder="Type legal question...">{{ old('question') }}</textarea><div class="form-actions"><button class="btn-primary"><i class="fas fa-paper-plane"></i> Ask</button></div></div></form>
<div class="page-card"><div class="page-card-table"><table class="min-w-full datatable datatable-LegalQa"><thead><tr><th></th><th>ID</th><th>Question</th><th>User</th><th>Status</th><th>Date</th><th style="text-align:right;">Actions</th></tr></thead><tbody>@foreach($legalQas as $legalQa)<tr data-entry-id="{{ $legalQa->id }}"><td></td><td>#{{ $legalQa->id }}</td><td>{{ \Illuminate\Support\Str::limit($legalQa->question, 90) }}</td><td>{{ $legalQa->user->name ?? 'System' }}</td><td>{{ ucfirst($legalQa->status) }}</td><td>{{ $legalQa->created_at->format('d M Y') }}</td><td><div class="action-row">@can('legal_qa_show')<a class="btn-outline" href="{{ route('admin.legal-qas.show', $legalQa) }}">View</a>@endcan @can('legal_qa_delete')<form method="POST" action="{{ route('admin.legal-qas.destroy', $legalQa) }}" style="display:inline" onsubmit="return confirm('{{ trans('global.areYouSure') }}')">@csrf @method('DELETE')<button class="btn-outline btn-outline-danger">Delete</button></form>@endcan</div></td></tr>@endforeach</tbody></table></div></div>
@endsection
@section('scripts')@parent<script>$(function(){initAdminDataTable('.datatable-LegalQa',{canDelete:@can('legal_qa_delete') true @else false @endcan,massDeleteUrl:"{{ route('admin.legal-qas.massDestroy') }}"});});</script>@endsection
